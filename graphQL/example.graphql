####
{

    data(func: has(id), first: 1) {
    id
    to:~to @facets(eq(id, "/r/RelatedTo"))@facets(id){
        id
    }

}
}
#####
{
	A as var(func: eq(id, "/c/en/0"))
 	M as var(func: eq(id, "/c/en/25/n/wn/quantity"))

 path as shortest(from: uid(A), to: uid(M)) {
  to
 }
 path(func: uid(path)) {
   id
 }
}